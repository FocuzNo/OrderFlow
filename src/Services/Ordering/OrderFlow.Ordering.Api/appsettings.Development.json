{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "OrderingDatabase": "Host=localhost;Port=5435;Database=ordering;Username=orderflow;Password=orderflow_dev_only"
  },

  "Kafka": {
    "BootstrapServers": "localhost:9092",
    "OrderCreatedTopic": "orderflow.order.created"
  },

  "Outbox": {
    "BatchSize": 20,
    "PollingIntervalMilliseconds": 2000,
    "MaxRetries": 10
  }
}
